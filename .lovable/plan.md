Auditoría de solo lectura completada arriba en el chat. No hay cambios de código que planear — el usuario pidió explícitamente "SOLO LECTURA, no modifiques nada".

Si tras revisar los hechos crudos quieres una acción de seguimiento, opciones típicas serían:
1. Descargar el PDF del poder de esta cancelación desde `expediente-files` y comparar la cédula `52310103` / escritura `1500` contra el documento real.
2. Agregar una regla de coherencia banco-poder vs banco-certificado (hoy ausente).
3. Persistir `prompt_version` por fila en `cancelaciones` para trazabilidad (deuda técnica identificada en la auditoría anterior).

Indica cuál (si alguna) quieres que planee en detalle.